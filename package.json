{
  "name": "consulting",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "yarn run prisma:clean; turbo run build",
    "dev": "turbo run dev --no-cache --parallel --continue",
    "vercel:build": "yarn run prisma:clean; turbo run build --filter=web",
    "serverprod": "yarn run prisma:clean; turbo run build --filter=server && yarn workspace server run start:prod",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules",
    "prisma:clean": "rm -rf node_modules/.prisma; prisma generate",
    "prisma:gen": "prisma generate",
    "prisma:push": "prisma push"
  },
  "dependencies": {
    "@google-cloud/storage": "^5.17.0",
    "@nestjs/schematics": "^8.0.10",
    "@prisma/client": "^3.13.0",
    "@stripe/react-stripe-js": "^1.7.0",
    "@stripe/stripe-js": "^1.24.0",
    "axios": "^0.24.0",
    "nanoid": "^3.2.0",
    "stripe": "^8.207.0",
    "turbo": "^1.2.6"
  },
  "devDependencies": {
    "@types/node": "16.11.11",
    "eslint": "7",
    "prisma": "^3.13.0",
    "typescript": "4.5.2"
  },
  "packageManager": "yarn@3.2.0",
  "preferUnplugged": false
}
