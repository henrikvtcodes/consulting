{
  "name": "types",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "private": true,
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --external @prisma/client",
    "dev": "tsup src/index.ts --format esm,cjs --watch --dts --external @prisma/client"
  },
  "devDependencies": {
    "tsconfig": "*"
  },
  "dependencies": {
    "tsup": "^5.12.7"
  }
}
